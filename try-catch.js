function checkAge(){
    const age = document.getElementById('age');
    const ageTxt = age.value;
    const errTxt = document.getElementById('error');
    
    try {
        console.log(first)    
    } catch (error) {
        console.log('ERROR',error)
        errTxt.innerHTML = 'ERRORR SIRR'
    }
    console.log('first')
}